import { LibrairieValidation } from './librairie-validation';

describe('LibrairieValidation', () => {
  it('should create an instance', () => {
    expect(new LibrairieValidation()).toBeTruthy();
  });
});
